import './LoadingSpinner.css';

export default function LoadingSpinner() {
  return (
    <div className="loading-spinner-wrapper flex items-center justify-center min-h-screen">
      <div className="spinner" />
    </div>
  );
}
